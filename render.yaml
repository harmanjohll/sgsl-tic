services:
  - type: web
    name: sgsl-hub
    runtime: python
    rootDir: sgsl-hub/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
