fastapi==0.111.0
uvicorn[standard]==0.30.1
numpy>=1.26.0
# Optional: needed only when STORAGE_BACKEND=s3 (DigitalOcean Spaces)
boto3>=1.34.0
# SMPL-X mesh generation (optional — only needed for generate_mesh.py)
# torch>=2.0.0
# smplx>=0.1.28
# trimesh>=4.0.0
